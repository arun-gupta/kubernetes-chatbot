CreateIntent create a kubernetes cluster
CreateIntent create a kubernetes cluster with {worker} worker
CreateIntent create a kubernetes cluster with {worker} worker nodes
CreateIntent create a kubernetes cluster with {master} master
CreateIntent create a kubernetes cluster with {master} master nodes
CreateIntent create a kubernetes cluster with {master} master and {worker} worker
CreateIntent create a kubernetes cluster with {master} masters and {worker} workers
CreateIntent create a kubernetes cluster with {master} master nodes and {worker} worker nodes

CreateIntent create a kubernetes cluster with {master} master, {worker} worker, in {region} region
CreateIntent create a kubernetes cluster with {master} master node, {worker} worker node, in {region} region
CreateIntent create a kubernetes cluster with {master} masters, {worker} workers, in {region} region
CreateIntent create a kubernetes cluster with {master} master nodes, {worker} worker nodes, in {region} region

CreateIntent create a kubernetes cluster with {master} masters, {worker} workers, in {region} region, and use {s3} bucket

ScaleIntent scale the kubernetes cluster {name} up by 2 worker nodes
ScaleIntent scale the kubernetes cluster {name} down by 2 worker nodes

DeleteIntent delete the kubernetes cluster {name}
